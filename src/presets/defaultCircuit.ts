/**
 * Default Circuit Specification (Empty Workbench)
 */

import type { CanvasComponentInstance } from '@store/canvasStore';
import type { CircuitEdge } from '@graph/types';

export const DEFAULT_COMPONENTS: CanvasComponentInstance[] = [];
export const DEFAULT_EDGES: CircuitEdge[] = [];
